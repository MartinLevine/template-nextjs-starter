# Martin Levine's NextJS Starter Template

Notice: first of all, run this command:

```bash
pnpm dlx prisma init --url file:./data/dev.db --output ../src/database/prisma --datasource-provider sqlite
```